# 面向导师或领导的周报助手

`dvisor-manager-weekly-report` 是一个面向学生、科研人员和职场人士的 Codex Skill。它学习导师或领导的偏好和参考模板，根据本周研究或工作材料生成内容可核验、格式可编辑的周报。

> **适用范围声明：** 本 Skill 主要针对文本类周报，当前调优重点是正文内容、语言风格和文档排版，尚未针对 PPT/PPTX 形式进行专项调优。如需生成 PPT/PPTX，可在相应环境已安装 `$ppt-master` 时调用该 Skill，但生成后的版式和展示效果仍需另行检查与调整。

## 发布与安装说明

> **第三方发布声明：** 本项目目前未提交至 OpenAI 官方插件目录，也未经过 OpenAI 官方发布审核或背书。本项目作为第三方开源 Codex Skill 通过 GitHub 提供。建议用户在安装前查看 `SKILL.md`、`agents/openai.yaml` 和 `references/` 中的文件，确认其内容符合自己的使用需求。

### 从 GitHub 安装

本 Skill 无需进入 OpenAI 官方插件目录即可从 GitHub 安装。请将下面的完整指令发送给 Codex：

```text
请使用 $skill-installer 从以下 GitHub 目录安装 dvisor-manager-weekly-report：
https://github.com/lyugaoxin-boop/dvisor-manager-weekly-report/tree/main/skills/dvisor-manager-weekly-report
```

安装成功后，请在下一轮对话或新的对话中调用：

```text
$dvisor-manager-weekly-report
```

如果安装后没有立即显示该 Skill，请重新启动 Codex，然后再次尝试调用。

### 为什么首次安装需要完整链接

由于本项目尚未进入 OpenAI 官方插件目录，Codex 无法保证仅根据以下名称找到对应的 GitHub 下载地址：

```text
dvisor-manager-weekly-report
```

因此，首次安装时需要同时提供上面的 GitHub Skill 目录链接。安装完成后，后续使用不再需要重复提供链接。

安装器实际下载的是仓库中的以下目录：

```text
skills/dvisor-manager-weekly-report/
```

仓库根目录中的 `.codex-plugin/plugin.json` 用于 Plugin 打包、安装测试及后续发布，不影响通过 GitHub 目录直接安装 Skill。

## 核心能力

- 从偏好描述、导师/领导模板、师兄师姐/同事模板中学习风格。
- 用户未说明参考模板来源时，全部默认视为导师/领导模板；用户之后明确说明的来源始终覆盖该默认分类。
- 按需进行一问一答式澄清，并解释每个问题的原因。
- 在完整生成前展示风格示例，等待用户明确确认。
- 支持论文、实验、图片、表格、项目记录、业务指标和风险等材料。
- 每篇论文调研默认给出必要公式、实验设置（含数据集介绍）和实验结果：解释关键公式与符号，说明数据来源、规模、划分及关键对比条件，并列出具体指标和结果。仿真或案例研究介绍实际测试系统；论文没有相应内容时不编造，重要缺口在预览前按需澄清。论文中的结果与用户自己的实验明确区分。
- 按最小有意义的内容单元匹配篇幅：每篇论文、每组实验结论、每项代码/项目结论分别参照模板中的同类单元；单元越多，章节总篇幅相应增加，而不是机械套用模板章节或整篇周报的固定长度。
- 分别学习文档标题及一级、二级、三级等各级标题的字号、字体、加粗、编号、缩进、间距和分页行为，避免把不同层级压成同一种格式。
- 从多个正文样本中学习段落行距类型及数值、段前段后间距，并在模板稳定区分时分别复现普通正文、列表正文和表格内正文的段落规则。
- 把语言风格作为独立重点学习项，包括句子长短、信息密度、先结论还是先过程、用词、语气、技术细节密度以及进展、结果、风险和下一步的表达方式。
- 默认把读者视为没有相关背景但理解力强的人，用简单、直接、自然的语言说清核心观点、证据和逻辑；模板语言风格只在这一基础上调整语气、详略和组织方式。
- 不照搬模板中的晦涩行业术语、内部黑话、管理口号或生硬翻译腔。必须保留的专业名词在首次出现时用日常语言解释其实际含义。
- 资料检索、结构设计和逻辑核对保持专业严谨，但周报不展示构思过程，只呈现整理后的结论、证据链和必要解释。
- 证据不足和不允许使用的更强结论保留在内部证据记录中；周报只写当前有依据的结果，不写“缺少哪些设置，因此不能判断或不能声称什么”这类审计式说明。
- 图题和表题优先严格沿用模板样式；模板没有可参考样式时，默认比正文小一个常规字号级别。
- 公式字号优先沿用已确认模板或用户要求；两者都没有规定时，默认与正文字号一致。
- 论文默认标注为：`英文论文题目（期刊名称；年份；最新新锐分区；影响因子；通讯作者；国家单位）`。六项信息按此顺序放在同一对括号中，以分号分隔，不另起段落重复列出；年份指论文发表年份，分区版本与 JIF 年份各自标在对应字段内，国家单位写成“国家—单位”。英文周报仍按原规则使用 JCR 分区，字段顺序不变；不翻译论文题名、不显示 DOI。
- 默认保留全部非重复的图片和结果表，并配标题和分析。
- 发现表格截图时先询问是否还原为可编辑表格，并逐单元格核对。
- 已提供原生可编辑表格，或截图已经还原为可编辑表格时，周报不再重复放表格截图，除非用户明确要求两者都保留。
- 周报只分析图表数据和结论，不写“将截图还原为可编辑表格”等文档制作说明。
- 图表横纵轴文字与正文语种不一致时，保留原图并生成同正文语种的轴文字翻译版。
- 内部建立证据记录，防止数字错误、结论夸大和状态误报。
- 只学习模板中的结构和样式，不复用“汇报人、答辩人、姓名、学号、项目”等具体内容值；本次未提供汇报人、答辩人等人员身份时默认填写 `xxx`。
- 主要面向 DOCX、PDF、Markdown 等文本类周报；PPT/PPTX 可在相应环境中调用 `$ppt-master` 生成，但尚未进行专项调优。

## 调用方法

```text
$dvisor-manager-weekly-report
```

使用示例：

```text
$dvisor-manager-weekly-report

汇报对象：课题组导师
导师偏好：先说结论，重视实验对比和失败原因，不喜欢大段背景介绍。
导师模板：templates/advisor-template.docx
本周材料：weekly-input/2026-week-36/

请先按需询问你无法确定的关键信息。确认目标后给出风格示例，得到我的明确确认后再生成完整周报。
```

## 使用前需要准备的材料

建议至少提供以下两类信息：

1. **风格依据：** 汇报对象的文字偏好、导师/领导模板或师兄师姐/同事模板，至少提供一种。
2. **本周材料：** 可以是论文、实验结果、图片、表格、项目记录、业务指标、风险说明或下一步计划。

如果仅提供文字偏好而没有文件模板，Skill 会询问最终输出格式。用户未说明模板来源时，默认将其视为导师或领导模板。

## 风格依据优先级

```text
本次周报临时要求
> 用户描述的导师/领导偏好
> 导师/领导模板
> 师兄师姐/同事模板
```

三种长期风格依据至少提供一种。用户本次明确提出的临时要求具有最高优先级。

## 默认工作流程

1. 检查用户提供的风格依据和本周材料。
2. 仅针对会实质影响格式、结构、重点、证据或交付结果的问题进行澄清。
3. 每次只询问一个问题，并说明该问题为什么会影响最终结果。
4. 向用户复述已经理解的目标、采用的默认设置和非阻塞性假设，等待确认。
5. 提供风格示例，等待用户明确确认。
6. 根据确认后的要求生成完整周报，并核对内容、数据、结论和版式。

## 默认交付

- **周报成品：** 始终交付。
- **风格画像：** 首次建立或更新时交付。
- **证据记录：** 内部使用，用户明确要求时才交付。

## 仓库结构

```text
dvisor-manager-weekly-report/
├── .codex-plugin/
│   └── plugin.json
├── skills/
│   └── dvisor-manager-weekly-report/
│       ├── SKILL.md
│       ├── agents/
│       │   └── openai.yaml
│       └── references/
│           ├── quality-checklist.md
│           ├── report-production-rules.md
│           └── style-profile-schema.md
├── .gitignore
├── LICENSE
└── README.md
```

## 更新说明

如果仓库内容已经更新，但本地仍在使用旧版本，请重新安装该 Skill，或删除原有安装后再从 GitHub 目录安装最新版本。更新后如未立即生效，请重新启动 Codex。

## 许可证

本项目采用 MIT License，具体内容见 [LICENSE](LICENSE)。

## 相关文档

- [OpenAI：Build skills](https://learn.chatgpt.com/docs/build-skills)
- [GitHub 仓库](https://github.com/lyugaoxin-boop/dvisor-manager-weekly-report)
